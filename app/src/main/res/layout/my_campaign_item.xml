<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <androidx.cardview.widget.CardView
        android:id="@+id/my_campaign_thumbnail_container"
        android:layout_width="150dp"
        android:layout_height="150dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:cardCornerRadius="12dp"
        app:cardElevation="0dp">

        <ImageView
            android:id="@+id/my_campaign_thumbnail"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            tools:src="@android:drawable/ic_menu_save"
            />

        <ImageView
            android:id="@+id/my_campaign_donate_done"
            android:layout_width="65dp"
            android:layout_height="65dp"
            android:src="@drawable/btn_donation_med_n"
            android:layout_gravity="center"
            android:visibility="invisible"
            />

    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/my_campaign_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:textSize="16sp"
        app:layout_constraintTop_toBottomOf="@id/my_campaign_thumbnail_container"
        app:layout_constraintStart_toStartOf="@id/my_campaign_thumbnail_container"
        app:layout_constraintEnd_toEndOf="@id/my_campaign_thumbnail_container"
        tools:text="세상에서 제일 쉬운 나눔 "
        />

</androidx.constraintlayout.widget.ConstraintLayout>